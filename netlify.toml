[build]
  functions = "netlify/functions"

# Optional: makes /.netlify/functions/submit available as /api/submit
[[redirects]]
  from = "/api/submit"
  to = "/.netlify/functions/submit"
  status = 200